import PublicSearchPage from '../public_pages/SearchPage';

// Reuse the public search page UI, but rendered inside user layout
const UserSearchPage = () => {
  return <PublicSearchPage />;
};

export default UserSearchPage;
